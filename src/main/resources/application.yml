server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Boeken
    username: root
    password:
    driver-class_name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true